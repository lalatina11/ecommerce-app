import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'iShop Admin | Dashboard',
    description: "iShop Admin page",
    icons:"logo.svg",
}
const Page = () => {
    return (
        <div>Dashboard Page</div>
    )
}

export default Page;